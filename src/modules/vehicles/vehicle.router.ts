import { Router } from 'express';

/**
 * Vehicle router stub — placeholder until task 8 implements the full router.
 * Mounted at /api/vehicles in app.ts.
 */
const vehicleRouter = Router();

export { vehicleRouter };
